import React from 'react';
import { Navigate } from 'react-router-dom';

const AdminModerateRequests = () => (
  <Navigate to="/administration-moderation" replace />
);

export default AdminModerateRequests;
